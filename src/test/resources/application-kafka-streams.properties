kafka.stream.topic-in= streaming-topic-in
kafka.stream.topic-out= streaming-topic-out
kafka.stream.filtering-topic-in=filtering-topic-in
kafka.stream.filtering-topic-out=filtering-topic-out
spring.kafka.bootstrap-servers=${spring.embedded.kafka.brokers}
spring.kafka.streams.bootstrap-servers=${spring.embedded.kafka.brokers}
spring.kafka.streams.application-id=kafka-stream
